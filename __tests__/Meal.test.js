import MealDB from '../src/Meal.js';

// Method successfully fetches a random meal from the API

describe('MealDB', () => {
  it('should fetch a random meal', async () => {
    const mealDB = new MealDB();
   
    );
   
  });

